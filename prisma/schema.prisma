generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
  schemas   = ["auth", "public"]
}

model UserVideoState {
  id                     String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                String         @db.Uuid
  videoId                String         @map("video_id") @db.Uuid
  is_in_ideation         Boolean        @default(true)
  user_note              String?
  /// Tiptap JSON for the Notion-style side editor (Phase 1-4 MVP).
  /// Source of truth for rich notes. `user_note` is kept as a dual-written plain-text extract
  /// so that the existing eviction rule (`user_note IS NULL`) continues to work unchanged.
  user_note_json         Json?          @map("user_note_json") @db.JsonB
  watch_position_seconds Int?           @default(0)
  is_watched             Boolean?       @default(false)
  cell_index             Int?           @default(-1)
  level_id               String?        @default("scratchpad")
  mandala_id             String?        @db.Uuid
  sort_order             Int?
  /// True when the row was inserted by the auto-add pipeline (recommendation_cache → user_video_states).
  /// Selective replace of stale recs only deletes WHERE auto_added=true AND user_note IS NULL
  /// AND is_watched=false AND watch_position_seconds=0 AND is_in_ideation=false.
  /// User trace on an auto-added row promotes it to permanent (preserved across refreshes).
  auto_added             Boolean        @default(false)
  added_to_ideation_at   DateTime       @default(now()) @db.Timestamptz(6)
  createdAt              DateTime       @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime       @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  users                  users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  video                  youtube_videos @relation(fields: [videoId], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala                user_mandalas? @relation(fields: [mandala_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([user_id, videoId])
  @@index([user_id], map: "idx_user_video_states_user_id")
  @@index([videoId], map: "idx_user_video_states_video_id")
  @@index([mandala_id], map: "idx_user_video_states_mandala_id")
  @@index([mandala_id, cell_index, auto_added], map: "idx_user_video_states_auto_add_lookup")
  @@map("user_video_states")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String    @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  email           String?   @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      users               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@index([oauth_client_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model users {
  instance_id                 String?                @db.Uuid
  id                          String                 @id @db.Uuid
  aud                         String?                @db.VarChar(255)
  role                        String?                @db.VarChar(255)
  email                       String?                @db.VarChar(255)
  encrypted_password          String?                @db.VarChar(255)
  email_confirmed_at          DateTime?              @db.Timestamptz(6)
  invited_at                  DateTime?              @db.Timestamptz(6)
  confirmation_token          String?                @db.VarChar(255)
  confirmation_sent_at        DateTime?              @db.Timestamptz(6)
  recovery_token              String?                @db.VarChar(255)
  recovery_sent_at            DateTime?              @db.Timestamptz(6)
  email_change_token_new      String?                @db.VarChar(255)
  email_change                String?                @db.VarChar(255)
  email_change_sent_at        DateTime?              @db.Timestamptz(6)
  last_sign_in_at             DateTime?              @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?              @db.Timestamptz(6)
  updated_at                  DateTime?              @db.Timestamptz(6)
  phone                       String?                @unique
  phone_confirmed_at          DateTime?              @db.Timestamptz(6)
  phone_change                String?                @default("")
  phone_change_token          String?                @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?              @db.Timestamptz(6)
  confirmed_at                DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                @default("") @db.VarChar(255)
  email_change_confirm_status Int?                   @default(0) @db.SmallInt
  banned_until                DateTime?              @db.Timestamptz(6)
  reauthentication_token      String?                @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?              @db.Timestamptz(6)
  is_sso_user                 Boolean                @default(false)
  deleted_at                  DateTime?              @db.Timestamptz(6)
  is_anonymous                Boolean                @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  sessions                    sessions[]
  user_local_cards            user_local_cards[]
  user_mandalas               user_mandalas[]
  mandala_subscriptions       mandala_subscriptions[]
  content_entities            content_entities[]
  user_subscriptions          user_subscriptions[]
  user_ui_preferences         user_ui_preferences[]
  user_llm_keys               user_llm_keys[]
  UserVideoState              UserVideoState[]
  youtube_playlists              youtube_playlists[]
  source_mandala_mappings     source_mandala_mappings[]
  youtube_sync_settings          youtube_sync_settings[]
  youtube_channels               youtube_channels[]
  youtube_subscriptions          youtube_subscriptions[]
  youtube_watch_history          youtube_watch_history[]
  youtube_watch_history_imports  youtube_watch_history_imports[]
  card_snapshots                 card_snapshots[]
  bot_usage_log                  bot_usage_log[]
  bot_approval_tokens            bot_approval_tokens[]
  newsletter_settings            newsletter_settings?
  newsletter_logs                newsletter_logs[]
  skill_runs                     skill_runs[]
  skill_outputs                  skill_outputs[]
  mandala_likes                  mandala_likes[]
  skill_configs                  user_skill_config[]
  recommendation_cache           recommendation_cache[]
  recommendation_feedback        recommendation_feedback[]
  generation_log                 generation_log[]

  @@index([instance_id])
  @@index([is_anonymous])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_mandalas {
  id         String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String                @db.Uuid
  title      String                @default("My Mandala")
  is_default Boolean               @default(true)
  is_public    Boolean               @default(false)
  is_template  Boolean               @default(false)
  domain       String?
  language     String?               @db.VarChar(10)
  like_count   Int                   @default(0)
  clone_count  Int                   @default(0)
  share_slug   String?               @unique
  position     Int                   @default(0)
  created_at   DateTime              @default(now()) @db.Timestamptz(6)
  updated_at   DateTime              @default(now()) @db.Timestamptz(6)
  users            users                      @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  levels           user_mandala_levels[]
  videoStates      UserVideoState[]
  localCards       user_local_cards[]
  subscriptions    mandala_subscriptions[]
  activity_logs    mandala_activity_log[]
  contentEntities  content_entities[]
  sourceMappings   source_mandala_mappings[]

  mandala_shares   mandala_shares[]
  skill_outputs    skill_outputs[]
  mandala_likes    mandala_likes[]
  skill_configs    user_skill_config[]
  recommendation_cache recommendation_cache[]

  source_template_id String? @db.Uuid

  @@index([user_id], map: "idx_user_mandalas_user_id")
  @@index([is_public], map: "idx_user_mandalas_is_public")
  @@index([is_public, is_template], map: "idx_user_mandalas_explore")
  @@index([is_template, language, domain], map: "idx_user_mandalas_explore_filter")
  @@index([domain], map: "idx_user_mandalas_domain")
  @@index([like_count], map: "idx_user_mandalas_like_count")
  @@index([language], map: "idx_user_mandalas_language")
  @@schema("public")
}

/// Mandala share links — controls visibility, expiry, and PIN protection
model mandala_shares {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mandala_id  String    @db.Uuid
  share_code  String    @unique
  mode        String    @default("view") // 'view' | 'view_cards' | 'clone'
  pin_hash    String?
  expires_at  DateTime? @db.Timestamptz(6)
  created_at  DateTime  @default(now()) @db.Timestamptz(6)

  mandala user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([mandala_id])
  @@schema("public")
}

/// Mandala likes — tracks which users liked which public mandalas
model mandala_likes {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String   @db.Uuid
  mandala_id String   @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)

  users   users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, mandala_id])
  @@index([mandala_id], map: "idx_mandala_likes_mandala")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_mandala_levels {
  id              String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mandala_id      String               @db.Uuid
  parent_level_id String?              @db.Uuid
  level_key       String
  center_goal     String               @default("")
  center_label    String?
  subjects        String[]             @default([])
  subject_labels  String[]             @default([])
  position        Int                  @default(0)
  depth           Int                  @default(0)
  color           String?
  created_at      DateTime             @default(now()) @db.Timestamptz(6)
  updated_at      DateTime             @default(now()) @db.Timestamptz(6)
  mandala         user_mandalas        @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  parent          user_mandala_levels? @relation("level_hierarchy", fields: [parent_level_id], references: [id], onDelete: SetNull, onUpdate: NoAction)
  children        user_mandala_levels[] @relation("level_hierarchy")

  @@unique([mandala_id, level_key])
  @@index([mandala_id], map: "idx_user_mandala_levels_mandala_id")
  @@schema("public")
}

model source_mandala_mappings {
  id          String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id     String        @db.Uuid
  source_type String        // 'playlist', 'channel', 'hashtag'
  source_id   String        // youtube_playlist_id or channel_id
  mandala_id  String        @db.Uuid
  created_at  DateTime      @default(now()) @db.Timestamptz(6)

  user    users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala user_mandalas  @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, source_type, source_id, mandala_id])
  @@index([user_id], map: "idx_source_mandala_mappings_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mandala_subscriptions {
  id            String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  subscriber_id String        @db.Uuid
  mandala_id    String        @db.Uuid
  subscribed_at DateTime      @default(now()) @db.Timestamptz(6)
  users         users         @relation(fields: [subscriber_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala       user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([subscriber_id, mandala_id])
  @@index([subscriber_id], map: "idx_mandala_subscriptions_subscriber_id")
  @@index([mandala_id], map: "idx_mandala_subscriptions_mandala_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mandala_activity_log {
  id          String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  mandala_id  String        @db.Uuid
  user_id     String        @db.Uuid
  action      String
  entity_type String
  entity_id   String?       @db.Uuid
  metadata    Json?
  created_at  DateTime      @default(now()) @db.Timestamptz(6)
  mandala     user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([mandala_id, created_at(sort: Desc)], map: "idx_mandala_activity_log_mandala_created")
  @@index([user_id], map: "idx_mandala_activity_log_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_local_cards {
  id                   String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id              String         @db.Uuid
  url                  String
  title                String?
  thumbnail            String?
  link_type            String
  user_note            String?
  metadata_title       String?
  metadata_description String?
  metadata_image       String?
  cell_index           Int?           @default(-1)
  level_id             String?        @default("scratchpad")
  mandala_id           String?        @db.Uuid
  sort_order           Int?
  video_id             String?        @db.VarChar(11)
  created_at           DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?      @default(now()) @db.Timestamptz(6)
  users                users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala              user_mandalas? @relation(fields: [mandala_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([user_id, url])
  @@index([created_at(sort: Desc)], map: "idx_user_local_cards_created_at")
  @@index([level_id], map: "idx_user_local_cards_level_id")
  @@index([user_id], map: "idx_user_local_cards_user_id")
  @@index([mandala_id], map: "idx_user_local_cards_mandala_id")
  @@index([user_id, mandala_id], map: "idx_user_local_cards_user_mandala")
  @@index([video_id], map: "idx_user_local_cards_video_id")
  @@schema("public")
}

model video_summaries {
  video_id            String   @id
  url                 String
  title               String?
  summary_en          String?
  summary_ko          String?
  tags                String[]
  model               String?
  transcript_segments Int      @default(0)
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  updated_at          DateTime @default(now()) @updatedAt @db.Timestamptz(6)

  @@schema("public")
}

model content_entities {
  id          String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id     String         @db.Uuid
  source_type String
  title       String
  source_url  String
  source_id   String?
  thumbnail   String?
  notes       String?
  tags        String[]       @default([])
  metadata    Json?          @db.JsonB
  cell_index  Int?           @default(-1)
  level_id    String?        @default("scratchpad")
  mandala_id  String?        @db.Uuid
  sort_order  Int?
  created_at  DateTime       @default(now()) @db.Timestamptz(6)
  updated_at  DateTime       @default(now()) @db.Timestamptz(6)
  users       users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala     user_mandalas? @relation(fields: [mandala_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([user_id, source_url])
  @@index([user_id], map: "idx_content_entities_user_id")
  @@index([source_type], map: "idx_content_entities_source_type")
  @@index([mandala_id], map: "idx_content_entities_mandala_id")
  @@index([created_at(sort: Desc)], map: "idx_content_entities_created_at")
  @@schema("public")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_subscriptions {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id           String    @unique @db.Uuid
  tier              String?   @default("free")
  local_cards_limit Int?      @default(100)
  mandala_limit     Int?      @default(3)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  updated_at        DateTime? @default(now()) @db.Timestamptz(6)
  users             users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id], map: "idx_user_subscriptions_user_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_ui_preferences {
  id                       String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                  String   @unique @db.Uuid
  scratchpad_is_floating   Boolean  @default(false)
  scratchpad_dock_position String?  @default("top")
  scratchpad_position_x    Int?     @default(100)
  scratchpad_position_y    Int?     @default(100)
  scratchpad_width         Int?     @default(320)
  scratchpad_height        Int?     @default(320)
  mandala_is_floating      Boolean  @default(false)
  mandala_is_minimized     Boolean  @default(false)
  mandala_dock_position    String?  @default("left")
  mandala_position_x       Int?     @default(100)
  mandala_position_y       Int?     @default(80)
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  updated_at               DateTime @default(now()) @db.Timestamptz(6)
  users                    users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id], map: "idx_user_ui_preferences_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_llm_keys {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String   @db.Uuid
  provider       String   @db.VarChar(50)
  encrypted_key  String
  status         String   @default("active") @db.VarChar(20)
  priority       Int      @default(0)
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_at     DateTime @default(now()) @db.Timestamptz(6)
  users          users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, provider])
  @@index([user_id], map: "idx_user_llm_keys_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model youtube_playlist_items {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  playlist_id       String            @db.Uuid
  video_id          String            @db.Uuid
  position          Int               @default(0)
  added_at          DateTime          @default(now()) @db.Timestamptz(6)
  removed_at        DateTime?         @db.Timestamptz(6)
  youtube_playlists youtube_playlists @relation(fields: [playlist_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  youtube_videos    youtube_videos    @relation(fields: [video_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([playlist_id, video_id])
  @@index([playlist_id], map: "idx_youtube_playlist_items_playlist_id")
  @@index([video_id], map: "idx_youtube_playlist_items_video_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model youtube_playlists {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                String                   @db.Uuid
  youtube_playlist_id    String
  youtube_playlist_url   String
  title                  String?
  description            String?
  thumbnail_url          String?
  channel_title          String?
  item_count             Int?                     @default(0)
  last_synced_at         DateTime?                @db.Timestamptz(6)
  sync_status            String?                  @default("pending")
  sync_error             String?
  is_paused              Boolean                  @default(false)
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at             DateTime                 @default(now()) @db.Timestamptz(6)
  users                  users                    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sync_schedules         sync_schedules?
  youtube_playlist_items youtube_playlist_items[]
  youtube_sync_history   youtube_sync_history[]

  @@unique([user_id, youtube_playlist_id])
  @@index([user_id], map: "idx_youtube_playlists_user_id")
  @@schema("public")
}

/// Mandala embeddings for vector search (Explore recommendations).
/// Uses pgvector extension. 18009 rows in Prod (2026-04-04).
model mandala_embeddings {
  id             BigInt                   @id @default(autoincrement())
  mandala_id     String
  level          Int
  sub_goal_index Int?
  text           String?
  embedding      Unsupported("vector(4096)")?
  language       String?                  @default("ko")
  domain         String?
  center_goal    String?
  center_label   String?
  sub_goal       String?
  sub_label      String?
  created_at     DateTime?                @default(now()) @db.Timestamptz(6)

  @@index([mandala_id], map: "idx_me_mid")
  @@index([level], map: "idx_me_lvl")
  @@index([language], map: "idx_me_lang")
  @@index([domain], map: "idx_me_dom")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model youtube_sync_history {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  playlist_id       String            @db.Uuid
  status            String
  started_at        DateTime          @default(now()) @db.Timestamptz(6)
  completed_at      DateTime?         @db.Timestamptz(6)
  items_added       Int?              @default(0)
  items_removed     Int?              @default(0)
  error_message     String?
  quota_used        Int?              @default(0)
  youtube_playlists youtube_playlists @relation(fields: [playlist_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([playlist_id], map: "idx_youtube_sync_history_playlist_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model youtube_sync_settings {
  id                       String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                  String    @unique @db.Uuid
  sync_interval            String    @default("manual")
  auto_sync_enabled        Boolean   @default(false)
  auto_summary_enabled     Boolean   @default(true)
  youtube_access_token     String?
  youtube_refresh_token    String?
  youtube_token_expires_at DateTime? @db.Timestamptz(6)
  youtube_email            String?
  connected_at             DateTime? @db.Timestamptz(6)
  created_at               DateTime  @default(now()) @db.Timestamptz(6)
  updated_at               DateTime  @default(now()) @db.Timestamptz(6)
  users                    users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model youtube_videos {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  youtube_video_id       String                   @unique
  title                  String
  description            String?
  thumbnail_url          String?
  channel_title          String?
  duration_seconds       Int?
  published_at           DateTime?                @db.Timestamptz(6)
  view_count             BigInt?
  like_count             BigInt?
  created_at             DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at             DateTime                 @default(now()) @db.Timestamptz(6)
  userState              UserVideoState[]
  video_captions         video_captions[]
  video_notes            video_notes[]
  watch_sessions         watch_sessions[]
  youtube_playlist_items youtube_playlist_items[]
  youtube_watch_history  youtube_watch_history[]

  @@schema("public")
}

model youtube_channels {
  id                   String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id              String                  @db.Uuid
  youtube_channel_id   String
  title                String
  description          String?
  thumbnail_url        String?
  subscriber_count     BigInt?
  video_count          BigInt?
  custom_url           String?
  subscribed_at        DateTime?               @db.Timestamptz(6)
  created_at           DateTime                @default(now()) @db.Timestamptz(6)
  updated_at           DateTime                @default(now()) @db.Timestamptz(6)
  users                users                   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  youtube_subscriptions youtube_subscriptions[]

  @@unique([user_id, youtube_channel_id])
  @@index([user_id], map: "idx_youtube_channels_user_id")
  @@schema("public")
}

model youtube_subscriptions {
  id              String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id         String            @db.Uuid
  channel_id      String            @db.Uuid
  is_active       Boolean           @default(true)
  sync_new_videos Boolean           @default(false)
  last_synced_at  DateTime?         @db.Timestamptz(6)
  created_at      DateTime          @default(now()) @db.Timestamptz(6)
  updated_at      DateTime          @default(now()) @db.Timestamptz(6)
  users           users             @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  channel         youtube_channels  @relation(fields: [channel_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, channel_id])
  @@index([user_id], map: "idx_youtube_subscriptions_user_id")
  @@index([channel_id], map: "idx_youtube_subscriptions_channel_id")
  @@schema("public")
}

model youtube_watch_history {
  id          String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id     String                        @db.Uuid
  video_id    String                        @db.Uuid
  watched_at  DateTime                      @db.Timestamptz(6)
  source      String                        @default("manual")
  import_id   String?                       @db.Uuid
  created_at  DateTime                      @default(now()) @db.Timestamptz(6)
  users       users                         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  video       youtube_videos                @relation(fields: [video_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  import      youtube_watch_history_imports? @relation(fields: [import_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@unique([user_id, video_id, watched_at])
  @@index([user_id], map: "idx_youtube_watch_history_user_id")
  @@index([video_id], map: "idx_youtube_watch_history_video_id")
  @@index([import_id], map: "idx_youtube_watch_history_import_id")
  @@schema("public")
}

model youtube_watch_history_imports {
  id             String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String                  @db.Uuid
  source_type    String
  file_name      String?
  total_count    Int                     @default(0)
  imported_count Int                     @default(0)
  skipped_count  Int                     @default(0)
  status         String                  @default("pending")
  error_message  String?
  created_at     DateTime                @default(now()) @db.Timestamptz(6)
  updated_at     DateTime                @default(now()) @db.Timestamptz(6)
  users          users                   @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  watch_history  youtube_watch_history[]

  @@index([user_id], map: "idx_youtube_watch_history_imports_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model credentials {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String   @unique @db.Uuid
  data       String
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model quota_operations {
  id             String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  quota_usage_id String      @db.Uuid
  operation_type String
  cost           Int
  timestamp      DateTime    @default(now()) @db.Timestamptz(6)
  quota_usage    quota_usage @relation(fields: [quota_usage_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([quota_usage_id], map: "idx_quota_operations_quota_usage_id")
  @@index([timestamp], map: "idx_quota_operations_timestamp")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model quota_usage {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  date             DateTime           @unique @db.Timestamptz(6)
  used             Int                @default(0)
  quota_limit      Int                @default(10000)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at       DateTime           @default(now()) @db.Timestamptz(6)
  quota_operations quota_operations[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sync_schedules {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  playlist_id       String            @unique @db.Uuid
  interval_ms       Int
  enabled           Boolean           @default(true)
  last_run          DateTime?         @db.Timestamptz(6)
  next_run          DateTime          @db.Timestamptz(6)
  retry_count       Int               @default(0)
  max_retries       Int               @default(3)
  created_at        DateTime          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime          @default(now()) @db.Timestamptz(6)
  youtube_playlists youtube_playlists @relation(fields: [playlist_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([next_run, enabled], map: "idx_sync_schedules_next_run_enabled")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model video_captions {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  video_id       String         @db.Uuid
  language       String
  text           String
  segments       String
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  updated_at     DateTime       @default(now()) @db.Timestamptz(6)
  youtube_videos youtube_videos @relation(fields: [video_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([video_id, language])
  @@index([video_id], map: "idx_video_captions_video_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model video_notes {
  id                String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  video_id          String         @db.Uuid
  user_id           String?        @db.Uuid
  timestamp_seconds Int
  content           String
  tags              String?
  created_at        DateTime       @default(now()) @db.Timestamptz(6)
  updated_at        DateTime       @default(now()) @db.Timestamptz(6)
  youtube_videos    youtube_videos @relation(fields: [video_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([video_id, timestamp_seconds], map: "idx_video_notes_video_id_timestamp")
  @@index([user_id], map: "idx_video_notes_user_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model watch_sessions {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  video_id       String         @db.Uuid
  user_id        String?        @db.Uuid
  started_at     DateTime       @db.Timestamptz(6)
  ended_at       DateTime       @db.Timestamptz(6)
  start_pos      Int
  end_pos        Int
  duration       Int
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  youtube_videos youtube_videos @relation(fields: [video_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([video_id, started_at], map: "idx_watch_sessions_video_id_started_at")
  @@index([user_id], map: "idx_watch_sessions_user_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type         String
  identifier            String    @unique
  name                  String
  client_id             String
  client_secret         String
  acceptable_client_ids String[]  @default([])
  scopes                String[]  @default([])
  pkce_enabled          Boolean   @default(true)
  attribute_mapping     Json      @default("{}")
  authorization_params  Json      @default("{}")
  enabled               Boolean   @default(true)
  email_optional        Boolean   @default(false)
  issuer                String?
  discovery_url         String?
  skip_nonce_check      Boolean   @default(false)
  cached_discovery      Json?
  discovery_cached_at   DateTime? @db.Timestamptz(6)
  authorization_url     String?
  token_url             String?
  userinfo_url          String?
  jwks_uri              String?
  created_at            DateTime  @default(now()) @db.Timestamptz(6)
  updated_at            DateTime  @default(now()) @db.Timestamptz(6)

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

model bot_approval_tokens {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String   @db.Uuid
  action_summary String
  preview_data   Json?
  token          String   @unique
  status         String   @default("pending")
  expires_at     DateTime @db.Timestamptz(6)
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  user           users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_bot_approval_tokens_user")
  @@index([token], map: "idx_bot_approval_tokens_token")
  @@schema("public")
}

model bot_usage_log {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?  @db.Uuid
  command    String
  metadata   Json?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  user       users?   @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_bot_usage_log_user_id")
  @@index([created_at(sort: Desc)], map: "idx_bot_usage_log_created_at")
  @@schema("public")
}

model card_snapshots {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String   @db.Uuid
  reason         String
  snapshot_data  Json
  status         String   @default("pending")
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  rolled_back_at DateTime? @db.Timestamptz(6)
  user           users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_card_snapshots_user_id")
  @@index([status], map: "idx_card_snapshots_status")
  @@schema("public")
}

model admin_audit_log {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  admin_id    String   @db.Uuid
  action      String   @db.VarChar(100)
  target_type String   @db.VarChar(50)
  target_id   String?  @db.Uuid
  old_value   Json?
  new_value   Json?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model admin_promotions {
  id                  String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code                String    @unique @db.VarChar(50)
  type                String    @db.VarChar(30)
  value               Json
  starts_at           DateTime? @db.Timestamptz(6)
  ends_at             DateTime? @db.Timestamptz(6)
  max_redemptions     Int?
  current_redemptions Int?      @default(0)
  is_active           Boolean?  @default(true)
  created_by          String?   @db.Uuid
  created_at          DateTime? @default(now()) @db.Timestamptz(6)
  updated_at          DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model user_promotion_redemptions {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id         String    @db.Uuid
  promotion_id    String    @db.Uuid
  redeemed_at     DateTime? @default(now()) @db.Timestamptz(6)
  applied_changes Json

  @@schema("public")
}

model content_reports {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  reporter_id     String    @db.Uuid
  target_type     String    @db.VarChar(30)
  target_id       String    @db.Uuid
  reason          String    @db.VarChar(50)
  description     String?
  status          String?   @default("pending") @db.VarChar(20)
  resolved_by     String?   @db.Uuid
  resolved_at     DateTime? @db.Timestamptz(6)
  resolution_note String?
  created_at      DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model payment_transactions {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id           String    @db.Uuid
  stripe_payment_id String?   @unique @db.VarChar(255)
  stripe_invoice_id String?   @db.VarChar(255)
  amount            Int
  currency          String?   @default("usd") @db.VarChar(3)
  status            String    @db.VarChar(30)
  description       String?
  created_at        DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

// --- Skill System --------------------------------------------------------

model video_rich_summaries {
  video_id      String   @id @db.VarChar(11)
  tier_required String   @default("free") @db.VarChar(10)
  one_liner     String?
  structured    Json?
  quality_score Float?
  quality_flag  String?  @default("pending") @db.VarChar(20)
  model         String?  @db.VarChar(50)
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  updated_at    DateTime @default(now()) @updatedAt @db.Timestamptz(6)

  @@schema("public")
}

model newsletter_settings {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id       String    @unique @db.Uuid
  is_subscribed Boolean   @default(true)
  frequency     String    @default("weekly") @db.VarChar(10)
  day_of_week   Int       @default(1)
  send_hour     Int       @default(9)
  last_sent_at  DateTime? @db.Timestamptz(6)
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @updatedAt @db.Timestamptz(6)
  users         users     @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_newsletter_settings_user_id")
  @@schema("public")
}

model newsletter_logs {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String   @db.Uuid
  mandala_id     String?  @db.Uuid
  subject        String?
  curated_videos Json?
  sent_at        DateTime @default(now()) @db.Timestamptz(6)
  message_id     String?  @db.VarChar(200)
  status         String   @default("sent") @db.VarChar(20)
  users          users    @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_newsletter_logs_user_id")
  @@index([sent_at(sort: Desc)], map: "idx_newsletter_logs_sent_at")
  @@schema("public")
}

model skill_runs {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  skill_id    String    @db.VarChar(50)
  user_id     String?   @db.Uuid
  status      String    @default("running") @db.VarChar(20)
  input       Json?
  output      Json?
  error       String?
  retry_count Int       @default(0)
  started_at  DateTime  @default(now()) @db.Timestamptz(6)
  ended_at    DateTime? @db.Timestamptz(6)
  users       users?    @relation(fields: [user_id], references: [id], onDelete: SetNull)

  @@index([skill_id, status], map: "idx_skill_runs_skill_status")
  @@index([user_id], map: "idx_skill_runs_user_id")
  @@index([started_at(sort: Desc)], map: "idx_skill_runs_started_at")
  @@schema("public")
}

/// Skill output storage — generated content from skill executions
model skill_outputs {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id     String    @db.Uuid
  mandala_id  String    @db.Uuid
  skill_type  String    @db.VarChar(50)
  title       String
  content     String
  cell_scope  Int[]
  card_count  Int?
  model_used  String?   @db.VarChar(100)
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  users       users     @relation(fields: [user_id], references: [id], onDelete: Cascade)
  mandala     user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade)

  @@index([user_id, mandala_id], map: "idx_skill_outputs_user_mandala")
  @@index([created_at(sort: Desc)], map: "idx_skill_outputs_created_at")
  @@schema("public")
}

/// User skill configuration per mandala — controls which skills are enabled
model user_skill_config {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String   @db.Uuid
  mandala_id String   @db.Uuid
  skill_type String   @db.VarChar(50)
  enabled    Boolean  @default(false)
  config     Json     @default("{}")
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  users   users         @relation(fields: [user_id], references: [id], onDelete: Cascade)
  mandala user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade)

  @@unique([user_id, mandala_id, skill_type])
  @@index([mandala_id], map: "idx_user_skill_config_mandala")
  @@schema("public")
}

// ============================================================================
// Trend-based Video Recommendation Engine (Phase 0)
// Design: docs/design/insighta-trend-recommendation-engine.md
// Skill ID: video-discover
// ============================================================================

/// Layer 1: External trend signals (YouTube, Naver, Google Trends, HN)
/// Aggregated nightly via trend-collector skill.
model trend_signals {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  source      String   @db.VarChar(50)
  domain      String?  @db.VarChar(100)
  keyword     String   @db.VarChar(255)
  raw_score   Float
  norm_score  Float
  velocity    Float    @default(0)
  metadata    Json     @default("{}")
  language    String   @default("ko") @db.VarChar(10)
  fetched_at  DateTime @default(now()) @db.Timestamptz(6)
  expires_at  DateTime @db.Timestamptz(6)

  @@unique([source, keyword, language])
  @@index([keyword], map: "idx_trend_signals_keyword")
  @@index([expires_at], map: "idx_trend_signals_expires_at")
  @@index([source, fetched_at(sort: Desc)], map: "idx_trend_signals_source_fetched")
  @@schema("public")
}

/// Layer 2: Insighta Keyword Score (IKS) — 6-axis weighted score
/// Recomputed by IKS-scorer skill from trend_signals.
model keyword_scores {
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  keyword             String   @db.VarChar(255)
  domain              String?  @db.VarChar(100)
  language            String   @default("ko") @db.VarChar(10)
  iks_total           Float
  search_demand       Float?
  competition         Float?
  trend_velocity      Float?
  goal_relevance      Float?
  learning_value      Float?
  content_performance Float?
  weight_version      Int      @default(1)
  scored_at           DateTime @default(now()) @db.Timestamptz(6)
  expires_at          DateTime @db.Timestamptz(6)
  /// Phase 2b: Qwen3-Embedding-8B vector for the keyword text.
  /// Stored once by iks-scorer so Phase 3 video-discover can compute
  /// per-mandala similarity without re-calling Mac Mini Ollama at recommendation time.
  /// 4096d exceeds pgvector HNSW 2000d limit — brute-force cosine until volume forces a change.
  embedding           Unsupported("vector(4096)")?

  @@unique([keyword, language])
  @@index([iks_total(sort: Desc)], map: "idx_keyword_scores_iks_desc")
  @@index([domain, language], map: "idx_keyword_scores_domain_lang")
  @@index([expires_at], map: "idx_keyword_scores_expires_at")
  @@schema("public")
}

/// Layer 3: Recommendation cache — per (user, mandala, video) with TTL
/// Generated by video-discover skill.
model recommendation_cache {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id         String   @db.Uuid
  mandala_id      String   @db.Uuid
  cell_index      Int?
  keyword         String   @db.VarChar(255)
  domain          String?  @db.VarChar(100)
  video_id        String   @db.VarChar(64)
  title           String
  thumbnail       String?
  channel         String?  @db.VarChar(255)
  channel_subs    Int?
  view_count      Int?
  like_ratio      Float?
  duration_sec    Int?
  rec_score       Float
  iks_score       Float?
  trend_keywords  Json     @default("[]")
  rec_reason      String?
  status          String   @default("pending") @db.VarChar(20)
  weight_version  Int      @default(1)
  created_at      DateTime @default(now()) @db.Timestamptz(6)
  expires_at      DateTime @db.Timestamptz(6)
  /// YouTube publishedAt (CP360 — experiment #3). Persisted for post-hoc
  /// freshness auditing. Nullable because older rows predate this column.
  published_at    DateTime? @db.Timestamptz(6)

  users    users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  mandala  user_mandalas @relation(fields: [mandala_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  feedback recommendation_feedback[]

  @@unique([user_id, mandala_id, video_id])
  @@index([user_id, mandala_id, status], map: "idx_recommendation_cache_user_mandala_status")
  @@index([rec_score(sort: Desc)], map: "idx_recommendation_cache_rec_score_desc")
  @@index([expires_at], map: "idx_recommendation_cache_expires_at")
  @@index([keyword], map: "idx_recommendation_cache_keyword")
  @@schema("public")
}

/// Layer 4: User feedback on recommendations — drives accuracy aggregation
/// Actions: impression | click | watch_30s | add | memo | dismiss | later
model recommendation_feedback {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  recommendation_id String   @db.Uuid
  user_id           String   @db.Uuid
  action            String   @db.VarChar(20)
  action_score      Float
  created_at        DateTime @default(now()) @db.Timestamptz(6)

  recommendation recommendation_cache @relation(fields: [recommendation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users          users                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([recommendation_id], map: "idx_recommendation_feedback_rec")
  @@index([user_id, created_at(sort: Desc)], map: "idx_recommendation_feedback_user_created")
  @@index([action], map: "idx_recommendation_feedback_action")
  @@schema("public")
}

/// Race-fallback outcome log for mandala generation.
/// LoRA branch is captured even when it loses the race (background await),
/// so its output can feed fine-tuning data + LoRA-vs-LLM quality analysis.
/// See: docs/design/insighta-trend-recommendation-engine.md (eval pipeline)
model generation_log {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id                 String?   @db.Uuid
  goal                    String
  domain                  String?   @db.VarChar(100)
  language                String    @default("ko") @db.VarChar(10)

  // Race outcome
  lora_won                Boolean
  source_returned         String    @db.VarChar(20)

  // LoRA branch (captured even when race-lost)
  lora_output             Json?
  lora_duration_ms        Int?
  lora_valid              Boolean?
  lora_sub_goals          Int?
  lora_actions_total      Int?
  lora_action_unique_rate Float?
  lora_error              String?

  // LLM branch (null only when LoRA won and LLM was cancelled mid-flight)
  llm_output              Json?
  llm_duration_ms         Int?
  llm_valid               Boolean?
  llm_error               String?

  created_at              DateTime  @default(now()) @db.Timestamptz(6)

  users users? @relation(fields: [user_id], references: [id], onDelete: SetNull, onUpdate: NoAction)

  @@index([created_at(sort: Desc)], map: "idx_generation_log_created_at")
  @@index([lora_won], map: "idx_generation_log_lora_won")
  @@index([lora_valid], map: "idx_generation_log_lora_valid")
  @@index([user_id], map: "idx_generation_log_user")
  @@index([language], map: "idx_generation_log_language")
  @@schema("public")
}

/// Layer 5: Scoring weights history — versioned, human-approved
/// Updated by recommendation-tuner skill (weekly autoresearch).
/// Initial v1 weights set per design doc.
model scoring_weights {
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  version             Int      @unique
  search_demand       Float    @default(0.15)
  competition         Float    @default(0.10)
  trend_velocity      Float    @default(0.15)
  goal_relevance      Float    @default(0.25)
  learning_value      Float    @default(0.20)
  content_performance Float    @default(0.15)
  accuracy_before     Float?
  accuracy_after      Float?
  approved_by         String?  @db.VarChar(50)
  reason              String?
  active              Boolean  @default(false)
  created_at          DateTime @default(now()) @db.Timestamptz(6)

  @@index([active], map: "idx_scoring_weights_active")
  @@schema("public")
}
