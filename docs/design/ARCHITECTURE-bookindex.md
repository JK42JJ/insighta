# Insighta 아키텍처 SSOT — 북인덱스 중심 파이프라인

> **이 문서의 목적**: "v2 → 완성된 북인덱스(책) → 무엇이든 생성" 구조를 단일 진실원천으로 고정한다.
> **상태**: 정렬용 아키텍처 SSOT (구현 핸드오프 아님). 코드/DDL 0건.
> **거주지**: in-repo. 권장 경로 `docs/design/ARCHITECTURE-bookindex.md` — 메모리 의존은 망각을 보장하므로 반드시 레포에 박는다.
> **계보**: 이 구조는 새 개념이 아니다. 노트모드 북인덱스(CP447), hub-and-spoke master, 6/22 "완성데이터=북인덱스" 재정의에서 반복적으로 합의됐다. 본 문서는 그 합의 + 2026-06-23 영상-우선 구조 실측 + 8셀 뼈대/게이트 명료화를 하나로 합쳐 고정한 것이다.

---

## §0 한 줄 정의

**완성데이터 = 만다라 단위로 완성된 북인덱스(책)다.** v2가 재료, 북인덱스가 SSOT, 슬라이드덱은 그 책에서 파생되는 여러 산출물 중 하나일 뿐이다.

```
v2 (재료) → 북인덱스(완성된 책, SSOT) → ├ 슬라이드덱
                                          ├ 뉴스레터 / 리포트
                                          ├ 챗봇 컨텍스트
                                          ├ 지식그래프(KG)
                                          └ ... (책의 fan-out)
```

세션 내내 슬라이드만 보며 땜빵한 것이 잘못이었다 — **책을 안 보고 책의 한 출력만 고치려 한 것.** 책이 완성되면 슬라이드는 책의 충실한 투영이 된다. 책이 raw면 어떤 출력을 뽑아도 raw다.

---

## §0.5 관련도 채점 = 0층 (입력 신뢰성) — 2026-06-26 실측 / 2026-06-27 검증·확정

북인덱스 ③ 게이트(rel≥40 통과분 선별)는 **rel 점수를 신뢰 가능한 입력으로 가정**한다. rel이 변별 없으면 엉뚱한 카드가 통과 → 북인덱스 = 모래 위. 따라서 채점 = 북인덱스의 **0층**, §1④보다 선결.

### 측정 (prod 실측, user_video_states N=9108)
- 분포: avg **71.3** · sd 16.4 · p50 73 · **실사용 범위 35-92** (천장 92·바닥 35 — 프롬프트 "보수적/90+만 정확/0은 무관"이 양극 봉인).
- **게이트 rel≥40 통과 93%** = 거의 no-op (임계 40이 분포 중심 71과 안 맞음).
- per-mandala 내부 sd 평균 **12** (변별 30 / 뭉침 22 / 70 만다라). 만다라 avg 60.6~83.8 흩어짐.
- rubric 모드 = **이미 ON** (composition.ts sd≥12 목표 달성). → 변별 부재 아님, **임계 오교정 + 절대척도 압축**이 근본.
- **NULL 79%** (7178/9108) = **기능 ship(~06-08) 前 과거 카드.** 신규는 96% <1h 즉시 채점 = 워커 정상.

### 결정 (데이터 근거 + 2026-06-27 검증 후 확정)
- **D8 척도 re-anchor = 폐기.** 샘플 검증(N=39 동일카드 재채점): 절대 프롬프트 re-anchor가 sd **20.5→16.6 압축** + 저관련 **+17.7 인플레**(39→57) = **fragile.** 절대 튜닝은 클러스터를 **이동만** 시키고 변별을 못 만듦. → **채점 프롬프트·점수 무변경 확정** (절대 점수 그대로 사용).
- **D3 게이트 = 확정 (근본 레버).** 절대 점수 그대로 두고 per-mandala **median+ ∧ 절대 floor(35).** 시뮬(1893장/70만다라): median+ **60% 통과**(절대40 93% no-op 대비 의미), `>= median` **tie-pass로 뭉친 클러스터 보호**(전부 관련=전원 유지), floor가 무관 3.9% 탈락. 구현 = `passesBookGate(relevance, ctx{median,scoredCount}, cfg)` + `BOOK_GATE_MODE` flag, **단일 호출처(fill-book:238), 채점 무변경.**
- **§0.7 relevance_detail = 진단용 격하.** 채점 무변경이라 **축 write 비급** — 컬럼(3계층 적용 완료)은 향후 축 분포 측정용으로만 보존.
- **D4 NULL backfill = 독립.** 절대 점수 유지 → **re-anchor 대기 불필요**, NULL ~5000장 backfill 즉시 가능(현 로직). PASS_NULL=true 유지 → backfill 완료 후 false flip 시 상대 게이트가 전 카드 적용.

### baseline (re-anchor 전후 비교용, 2026-06-26)
`avg71.3 sd16.4 p50=73 range35-92 / 게이트40통과93% / per-mandala sd평균12(변별30·뭉침22) / NULL79%(과거)`

### 0단 범위 + 순서 (2026-06-27 축소)
**범위 = "채점 재설계" → "게이트 로직 교체"로 축소.** 채점(프롬프트·점수·워커)은 무변경. 0단 = **D3 게이트 교체(절대40 → 상대 median+ ∧ floor35) + D4 NULL backfill(독립)** 둘뿐. 순서: D3 게이트 구현(unset=inert) → D4 backfill → flag flip(`BOOK_GATE_MODE=relative` + `PASS_NULL=false`) → 그 위 §1④.

### D3 영향 격리 — 위저드-검색 무영향 증명 (2026-06-27, 회귀 불가 근거 영구 보존)
**James 절대조건: 위저드-검색 품질 회귀 = 수용 불가.** 코드로 **3중 격리** 증명:
1. **점수 무변경** — D3는 relevance_pct 값을 안 건드림(게이트 결정 로직만). 모든 reader(뱃지·정렬·풀·스냅샷)가 동일 점수.
2. **코드 격리** — `passesBookGate` 호출처 = **단일(fill-book:247).** 위저드/검색/발견/풀/pipeline 경로의 book-gate 참조 = **0 (grep 빈 결과).** 변할 코드 경로 없음.
3. **시간 격리** — fill-book은 book-build(book뷰 / #958 트리거) 시점 = 위저드 검색→발견→배치 **완료 後 하류.**

**relevance_pct reader 분류** (게이트 외 전부 무영향): fill-book(게이트=D3 변경) / cards.ts 뱃지(`mandala_relevance_pct` 별도 video-keyed 컬럼) / warm-select 스냅샷 segment(intra-video 지표) / pool-serve 자체 게이트(`goalContributionPct`, book-gate와 별개) / write paths(place-auto·enrich·segment = write, 게이트 read 아님).

**회귀 harness (기준선 `/tmp/gate-baseline.json`, 2026-06-27):**
- gate-영향 (book, 변해야): 1893장 / 70만다라 — 절대40 **93%** → 상대 median∧floor35 **60%.**
- gate-독립 (search, 불변이어야): `recommendation_cache` 7002 / `video_discover_traces` 5360 / uvs placement.
- **flip 後 baseline 재실행** → (a) book ~60% 확인(게이트 작동) (b) search 불변량 동일 확인(검색 무회귀) = 자동 검증.

### 0단 최종 결론 — 2026-06-27 James 도메인 판정 (D1 폐기, 채점 무변경)

조사 끝에 근본 진단이 두 번 정정됨:
- ~~"입력 빈약(title+목표 한 줄)"~~ (폐기) → ~~"센터골 단순 희석"~~ (폐기) → **"센터=목적경계 vs 셀=구체매칭 텐션".**
- 측정 격리(JWT-SpringBoot, 센터="Claude Code 로 상용 서비스 구축"): 셀만 92 / +센터 35 / +센터+형제 72. 센터의 35는 **희석이 아니라 목적경계 작동** — JWT-SpringBoot는 인증 셀엔 맞으나 Claude-Code 센터엔 안 맞음.
- **★ James 판정: JWT류 off-center 카드 탈락 = 정상(버그 아님). 채점은 옳게 작동 중.**

**확정:**
1. **D1 컨텍스트 채점 = 폐기.** "JWT 양질 오탈락" 전제가 틀림 → D1(형제 셀 컨텍스트) 불필요. **채점 워커 무변경.**
2. **0단 = D3 상대 게이트(ship 완료)로 충분.** 채점 점수·프롬프트·워커 전부 무변경.
3. **검색 = 무변경.** merged-gen(prod=true)이 센터+8셀+쿼리를 한 콜에 생성 = 이미 전체 맥락 보존.
4. **§0.7 `relevance_detail` 컬럼** = prod 3계층 존재하나 **미사용**(채점 무변경이라 축 write 비급). 무해 잔존 — DROP 안 함(향후 진단용).
5. **D4 backfill 재정의 무효화** — "D1 전체 재채점" 폐기. 채점 로직이 옳으므로 NULL은 **현 로직으로** backfill하면 됨(압축 우려 소멸).

**★ 남은 실제 이슈 (D1과 무관) — NULL 79% 게이트 우회:**
- D3 활성이나 `PASS_NULL=true`라 **채점된 21%에만 상대 게이트 작동, NULL 79%(ship 前 과거 카드)는 통과.**
- 해소: NULL ~5000장 **현 채점 로직으로 backfill**(~$66, 이제 "옳은 채점"이라 가치) → `PASS_NULL=false` flip = D3 전체 적용.
- **별도 [GO] 판단** (backfill 비용 vs 북인덱스 입력 완전성).

---

## §1 전체 파이프라인 (end-to-end)

```
┌──────────────────── INSIGHTA — 완성된 책 생산 ─────────────────────┐
│                                                                    │
│  ① 만다라 영상 배치 (~50개)        [EXISTS] 위저드/대시보드        │
│         │                                                          │
│         ▼                                                          │
│  ② 북인덱스 골격 = 8셀 뼈대        [부분] 셀 라벨(주제군)은 깨끗   │
│         │   (만다라 구조가 곧 책의 목차)                           │
│         ▼                                                          │
│  ③ 선별 게이트                     [NEW] 북인덱스에 기여하는       │
│         │   (관련도·조회수·품질로 조악 배제)   카드만 통과         │
│         ▼                                                          │
│  ④ 통과 카드만 v2 생성             [v2 EXISTS / 게이트연동 NEW]    │
│         │   (인덱스 기여분에만 비용 — 전수 v2 아님)                │
│         ▼                                                          │
│  ⑤ 내용차원 재배치 + 종합          [NEW] 영상 배치순서가 아니라    │
│         │   (8셀 안/사이를 내용 논리로)       내용 흐름으로 재구성  │
│         ▼                                                          │
│  ⑥ 인리치먼트 / 팩트체크 = '완성도' [NEW] 현재 enrichment=LLM      │
│         │   (균형 + 완성도 + 사실 검증)        자가생성뿐, 외부 0   │
│         ▼                                                          │
│  ◆ 완성된 북인덱스 (만다라 단위 SSOT)  ← distill-once 대상         │
│         │   (섹션이 figure 타겟을 보유 = "이 내용엔 이런 시각자료")│
│  (관련도 스코어 동행)              [EXISTS] A단계 + 구간 관련도    │
│                                                                    │
└────────────────────────────┬───────────────────────────────────┘
                             │  완성된 북인덱스 SSOT + 구간 관련도
                             ▼
┌──────────────── SLIDEGEN — 완성된 책 소비 → 시각화 ───────────────┐
│                                                                    │
│  ⑦ 관련도 게이트         [관련도 EXISTS] 구간 관련도 ≥ 임계만      │
│         │                  → 전수 select ✗ (비용 레버)             │
│         ▼                                                          │
│  ⑧ 스냅샷 수집 → 크롭 → numerize → render → 종합 슬라이드덱        │
│         │   (책의 figure 타겟이 수집을 주도 = on-demand)           │
│         ▼                                                          │
│  ◆ 슬라이드덱 (책의 한 소비처. docx·뉴스레터 어댑터도 같은 책 fan-out)│
│                                                                    │
└────────────────────────────────────────────────────────────────┘
```

---

## §2 북인덱스가 게이트다 (핵심 원리)

게이트는 별도 단계가 아니라 **북인덱스 그 자체**다.

- **뼈대 = 8개 셀.** 만다라니까 뼈대는 셀 구조다. 셀 라벨(주제군 8개)은 이미 깨끗하며, 이것이 "이 책이 담아야 할 내용"의 목차 골격이다.
- **배치 = 내용차원으로 재구성.** 영상이 어느 셀에 박혔느냐가 아니라, 내용이 어떻게 이어져야 하느냐로 순서/구성이 정해진다. 영상 경계를 넘어 내용 단위로 재배치돼도 된다. 순서는 내용차원으로 변경 가능.
- **선별 = 인덱스에 기여하는 카드만.** v2가 게이트가 아니다. 북인덱스가 "필요한 내용"을 정의하고, 그 기준으로 카드를 거른다. rel=5(예: "기술주 투자"가 자동화 만다라에), 저조회수 조악 콘텐츠는 인덱스 구성 단계에서 **배제** — 포함시키면 평균 품질이 떨어진다.
- **v2는 통과분에만.** v2를 다 만들어 놓고 쓰는 게 아니라, 인덱스에 기여할 카드만 v2 비용을 쓴다. (워밍/CV도 동일 — 슬라이드 안 될 영상까지 numerize = 낭비.)

이것이 VISION 원칙과 정합한다 — **"필터는 개선이 아니라 게이트여야 한다."** 단일 카드 시절엔 사람이 게이트였고(입력을 손으로 큐레이션), 만다라 자동화는 그 게이트를 시스템에 이식하는 것이다. 게이트 없이 양만 늘리면(만다라=많은 셀) 쓰레기도 비례해 늘어난다.

---

## §3 현재 상태 = 영상-우선 (2026-06-23 실측) — "내용→스냅샷"은 0% 미착공

코드/DB 측정으로 확정: 목표 구조("내용이 주체, 스냅샷/figure는 내용에 종속")는 **코드에 0% 구현**돼 있고, 전 사슬이 영상-우선이다.

| 단계 | 현재 (영상-우선) | 근거 (file:line / 테이블) |
|---|---|---|
| 배치 | 영상이 셀에 박힘 | `user_video_states.cell_index` |
| book 생성 | placed 영상 열거 → 섹션 = 영상 | `fill-book.ts:68-75` "Enumerate placed videos per cell" |
| 섹션 정체성 | `section.title = video.title` (영상 원제목) | `build-book.ts:128` (내용 라벨 아님) |
| figure 연결 | **book에 figure/media 필드 0건** | `build-book.ts` — 섹션이 figure를 참조/요구 안 함 |
| 워밍 | 영상 ts(고관련 구간) → 프레임 긁기 | `warm-select` → `get-or-extract(videoId, ts)` |
| collect | placed 영상 video_id로 snapshots 수집 | `collect-figures.ts` — 영상-키 평행 수집 |

**핵심**: book과 figure가 내용으로 연결되지 않는다. figure는 영상 기준으로 따로 모아 빌드 요청에 별첨될 뿐 — "내용이 스냅샷을 주도"가 아니라 "영상 프레임을 긁어 옆에 붙임"이다. content-first 인덱스(내용 항목을 영상과 독립 정의하고 그게 수집을 주도) = 0건.

---

## §4 결함은 하나의 뿌리 — "영상-우선"

2026-06-22~23 덱 실물 검수에서 나온 결함들은 별개 버그가 아니라 **영상-우선 단일 뿌리의 증상**이다. 그래서 슬라이드젠을 증상별로 땜빵하면 매번 다음 증상이 나온다.

| 결함 | 증상 | 뿌리 |
|---|---|---|
| 1 섹션 제목 = 유튜브 원제목 (이모지·채널·URL·해시태그·`(N/M)`) | 발표 제목이 클릭베이트 | 섹션 = 영상 (`build-book.ts:128`) |
| 2 무관 영상 셀 배치 (rel=5 placed) | 주식 영상이 자동화 만다라에 | 배치에 관련도 게이트 없음 |
| 3 figure 0 | CV 데모인데 시각 오브젝트 없음 | book에 figure 슬롯 0 + 영상-키 평행 수집 |
| 5 얇은 덱 | v2usable만 섹션화 → 1~2섹션 | 영상 우선 + v2 인리치 안 됨 |

→ 북인덱스 중심 구조(§1)로 전환하면 1·2·3·5가 뿌리에서 동시에 사라진다. (섹션=내용이지 영상 아님 / 게이트가 조악 배제 / 책이 figure 타겟 보유 / 완성도 레이어가 균형 확보.)

> **함정 기록 (2026-06-23)**: 데모로 빌드한 "AI 에이전트로 업무 자동화하기"(42706d64, 7섹션 thin)는 의도한 rich 942e2757(61섹션)이 **아니다.** 같은 제목 다른 만다라 = 동명 충돌. 빌드 전 `mandala_id`로 rich본 확정 필수.

---

## §5 figure 재배치 — distill 대상 아님, render-time on-demand

| v1.2 (폐기) | 재정의 (확정) |
|---|---|
| 완성데이터 = `video_figures` (영상 단위 figure 누적) | 완성데이터 = **완성된 북인덱스** (만다라 단위 문서) |
| CV를 데이터완성 시점에 전수 전진배치 | CV는 **slidegen이 관련도 높은 구간에 요청하는 on-demand** |
| distill-once 대상 = figure | distill-once 대상 = **북인덱스(텍스트)**. figure는 render-time 파생 |
| 선행 = figure 스키마 | 선행 = **북인덱스 스키마** (figure는 북인덱스·관련도 위에서만 의미) |

figure/스냅샷은 완성된 책을 입력으로, 책의 figure 타겟이 "이 내용을 보여줄 시각자료"를 주도해 수집한다. 비용을 위해 on-demand + 캐시. (figure-rank 프레임 선택 수정은 배포됨 — 영상에 figure가 있으면 잡고, 없으면 정직하게 0. 수율은 이제 코드가 아니라 콘텐츠가 정한다.)

---

## §6 레포 경계

- **경계 = 완성된 북인덱스.** insighta는 영상을 완성된 책으로 끌어올리는 데까지 책임. slidegen은 그 완성 책을 받아 시각화하는 데까지 책임.
- insighta는 slidegen이 받을 SSOT(북인덱스 + 구간 관련도)를 생산. slidegen은 그 책을 직접 수정하지 않는다(0파일 원칙 / book_json = SSOT).
- 이 문서가 REPO_BOUNDARIES.md의 미결 항목(James 경계 입력 대기)을 채운다.

---

## §7 무엇이 EXISTS / NEW (착공 지도)

| 구성 | 상태 |
|---|---|
| 만다라 영상 배치 | EXISTS (위저드/대시보드) |
| 8셀 뼈대 (셀 라벨) | EXISTS (깨끗) |
| 노트모드 북인덱스 개념 / `note_documents` 테이블 / MD·HTML·PDF export | EXISTS (CP447) |
| A단계 + 구간 관련도 스코어 | EXISTS |
| v2 (영상 단위 요약) | EXISTS |
| figure-rank 프레임 선택 | EXISTS (배포됨) |
| **선별 게이트 (관련도·조회수·품질로 카드 필터 → v2 대상 결정)** | **NEW** |
| **내용차원 재배치 + 종합 (8셀 균형·완성도)** | **NEW** |
| **인리치먼트 / 팩트체크 = '완성도' 레이어 (외부 리서치 포함)** | **NEW** |
| **북인덱스 섹션의 figure 타겟 보유 (book에 figure 슬롯)** | **NEW** |
| **수집을 내용 주도로 전환 (워밍/collect 입력 = 내용, not 영상 ts)** | **NEW** |

**선행 = "v2 → 완성된 북인덱스" 생성 층** (게이트 + 내용차원 재배치 + 인리치/팩트체크). 이게 서야 slidegen 시각화가 입력(완성된 책)을 받는다. slidegen 시각화(§1 ⑦⑧)는 완성 책이 나와야 입력이 생기므로 후순위.

---

## §8 불변 원칙 (이 구조가 흔들릴 때 돌아올 지점)

1. **완성데이터 = 북인덱스(책), not figure, not 영상 요약.**
2. **북인덱스가 게이트.** 기여 카드만 v2 — 전수 v2/전수 워밍 아님.
3. **뼈대 = 8셀, 배치 = 내용차원.** 섹션은 내용 단위지 영상 원제목 아님.
4. **책이 figure를 주도.** figure는 render-time on-demand 파생, distill 대상 아님.
5. **경계 = 완성된 북인덱스.** insighta 생산 / slidegen 소비.
6. **슬라이드는 fan-out의 하나.** 책을 고치지 슬라이드를 땜빵하지 않는다.
